#include <stdio.h>
int main() {
   int size;
   printf("Enter size of array\n");
   scanf("%d",&size);
   int arr[size];
   for (size_t i = 0; i < count; i++)
   {
    /* code */
   }
   
   printf
   return 0;
}
